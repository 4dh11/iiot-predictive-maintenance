apiVersion: v1
kind: Service
metadata:
  name: iiot-maintenance-service
spec:
  type: NodePort
  selector:
    app: iiot-maintenance
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080